# Excel to PDF Payslip Generator Requirements
# Core dependencies for reading Excel files and generating PDFs

# Data manipulation and Excel file reading
pandas>=1.3.0
openpyxl>=3.0.7
xlrd>=2.0.1

# PDF generation and styling
reportlab>=3.6.0

# GUI framework with drag & drop support
tkinterdnd2>=0.3.0

# Persian/Arabic text support (optional but recommended)
arabic-reshaper>=2.1.3
python-bidi>=0.4.2

# Note: tkinter is included with Python by default
# No additional installation required for tkinter